# Asset Resolver (Placeholder)

This folder is reserved for the future asset-resolver implementation.

The resolver will replace cache-path guessing with source identity and product-type resolution.

Planned functions:

- resolve source asset identity
- list products by source UUID
- verify expected product types
- report pending Asset Processor jobs
- emit resolver evidence into manifests
